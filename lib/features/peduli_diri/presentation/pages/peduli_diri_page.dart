import 'package:flutter/material.dart';

class PeduliDiriPage extends StatelessWidget {
  const PeduliDiriPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PeduliDiri'),
      ),
      body: const Center(
        child: Text('Halaman panduan PeduliDiri akan segera hadir.'),
      ),
    );
  }
}
